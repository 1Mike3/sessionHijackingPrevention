package proj.core.fingerprinting.comparators;

// same number as Encoding comparator because reference to external file
public class Cmp2_Encoding extends DefaultStringComparator implements FpComparator {
}
